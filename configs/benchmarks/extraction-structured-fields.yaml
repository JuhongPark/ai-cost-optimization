benchmark_id: extraction_structured_fields_v1
task_family: extraction
dataset: structured_field_extraction
description: Structured extraction benchmark for tool use and selective review policies.
acceptance_criteria:
  metric: f1
  threshold_type: minimum
  threshold_value: 0.9
secondary_criteria:
  precision_min: 0.92
  recall_min: 0.88
policy_priority:
  - selective_hitl
  - full_automation
  - human_first
recommended_policies:
  - strong_model_single_pass
  - cheap_model_single_pass
  - tool_assisted_extraction
  - verifier_gated_hitl
  - human_first
logging:
  schema: schemas/episode.schema.json
  output_dir: results/raw/extraction_structured_fields_v1
