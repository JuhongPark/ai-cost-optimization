# Provider Configs

This directory stores model-provider settings for real execution.

- `offline-heuristic.yaml`: local smoke-test provider that requires no network
- `openai-compatible.example.yaml`: template for an OpenAI-compatible HTTP endpoint
