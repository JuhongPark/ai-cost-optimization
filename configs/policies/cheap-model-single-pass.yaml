policy_id: cheap_model_single_pass
policy_class: full_automation
description: Cheap model baseline for first-pass cost comparison.
model_strategy:
  primary_model: cheap_model
  routing: none
retry_policy:
  enabled: false
verification_policy:
  enabled: false
human_oversight:
  mode: terminal_only
  escalation_trigger: final_failure_only
